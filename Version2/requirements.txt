# Existing dependencies (from your original script)
torch
pop_tools
xesmf
numpy
xarray
matplotlib

# New: required only if you use dest_grid="ease2_n25km" / build_ease2_n25km_grid()
pyproj

# New: required for io_utils.save_dataset(fmt="zarr") (default)
zarr

# Optional: only needed if you use io_utils.save_dataset(fmt="netcdf")
h5netcdf
